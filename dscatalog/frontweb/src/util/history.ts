import { createBrowserHistory } from 'history';

export default createBrowserHistory();

// tive problemas com o modulo history e precisei installar a biblioteca history na versao 4.10.1 para ter compatibilidade com o componente history do Router
// [yarn add history@4.10.1]